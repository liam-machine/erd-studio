.DS_Store
node_modules/
dist/
out/
out-webview/
*.vsix
.vscode-test/
dev-preview.html

# Local dbt project files for development testing
/dbt_project.yml
/target/
/models/
/selectors.yml
.env

# Git worktrees (per-branch checkouts, not for tracking)
.worktrees/

# Getting-started video pipeline: intermediates and its own dependencies
video/build/
video/node_modules/

# LinkedIn post/comment archive (personal, not for the repo)
.linkedin/

# GitHub traffic + install history (personal, see .traffic/log.mjs)
.traffic/

# Local-only Claude skill that reports on .traffic/ (not for the public repo)
.claude/skills/erd-traffic/
