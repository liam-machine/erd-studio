{
  "name": "erd-studio-feedback-proxy",
  "private": true,
  "version": "1.0.0",
  "description": "Cloudflare Worker behind the ERD Studio feedback-analysis hosted tier. Not part of the extension build.",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "test": "node --test test/worker.test.mjs",
    "check": "node --check src/index.js && npx --yes typescript@5 tsc --noEmit -p tsconfig.json",
    "dev": "npx --yes wrangler@4 dev",
    "deploy": "npx --yes wrangler@4 deploy",
    "tail": "npx --yes wrangler@4 tail --format pretty"
  },
  "engines": {
    "node": ">=18"
  }
}
