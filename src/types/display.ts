/**
 * Moved to `@erd-studio/core` (packages/core/src/types/display.ts). This module
 * re-exports every symbol so existing imports of this path keep working.
 */

export type {
  ManifestModelPreview,
  ExistingModelPreview,
  DisplayColumn,
  PhysicalColumnSource,
  PhysicalProvenance,
  DisplayModel,
  DisplayRelationship,
  DisplayDomain,
} from '@erd-studio/core';
