# Source and build config (not needed at runtime)
.github/**
.vscode/**
.vscode-test/**
src/**
webview/**
out/**
out-webview/**
test/**
node_modules/**
.gitignore
.eslintrc.json
tsconfig.json
tsconfig.webview.json
esbuild.js
vitest.config.ts
scripts/**

# Documentation and planning (not needed at runtime).
# Root-level markdown is excluded wholesale; only the marketplace-facing
# README / CHANGELOG (and LICENSE) are re-included below.
docs/**
.planning/**
*.md
!README.md
!CHANGELOG.md
features.json

# Sibling projects that live in this repo but are not part of the extension
forge-app/**
mcp-server/**
data engineer plugin/**

# Media only referenced from the external assets repo (README uses the
# raw.githubusercontent.com copy of demo.gif)
media/demo.gif

# Dev/local files
.claude/**
.env*
dev-preview.html
.worktrees/**
*.vsix
# Scratch files the deploy workflow creates next to package.json
marketplace.json
vsce-show.err
vsce-files.txt
.DS_Store
**/.DS_Store

# dbt files (for testing only)
models/**
target/**
dbt_project.yml
selectors.yml

# Source maps and TypeScript (keep bundle small)
**/*.map
**/*.ts

# Re-include dist (but maps already excluded above)
!dist/*.js
!dist/*.css
