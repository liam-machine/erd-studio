# Source and build config (not needed at runtime)
.vscode/**
.vscode-test/**
src/**
webview/**
out/**
out-webview/**
test/**
node_modules/**
.gitignore
.eslintrc.json
tsconfig.json
tsconfig.webview.json
esbuild.js
vitest.config.ts

# Documentation and planning (not needed at runtime)
docs/**
plan.md
features.json
CLAUDE.md

# Other projects accidentally in repo
data engineer plugin/**

# Dev/local files
.claude/**
.env*
dev-preview.html
.worktrees/**

# dbt files (for testing only)
models/**
target/**
dbt_project.yml

# Source maps and TypeScript (keep bundle small)
**/*.map
**/*.ts

# Re-include dist (but maps already excluded above)
!dist/*.js
!dist/*.css
