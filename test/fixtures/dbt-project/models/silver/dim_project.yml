version: 2
models:
  - name: dim_project
    description: Project dimension table
    config:
      tags:
        - silver
    columns:
      - name: project_id
        description: Surrogate key for project
      - name: project_name
        description: Project name
